#!/bin/bash
# MUNDO Agent — macOS .app 启动脚本

osascript <<'APPLESCRIPT'
tell application "Terminal"
    activate
    do script "cd ~/.hermes/mundo-agent && source venv/bin/activate && python3 mundo.py"
    set custom title of front window to "MUNDO"
end tell
APPLESCRIPT
